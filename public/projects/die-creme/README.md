# Die Creme, Berlin assets

Expected gallery files for this case study:

- `hero.png`
- `homepage-desktop.png`
- `menu-section.png`
- `reservation-flow.png`
- `mobile-view.png`
- `typography-colors.png`

These files are not available in the repo yet, so the page currently uses temporary remote placeholders to keep the build working. Replace the files above when the final screenshots are ready.
